## Telemetry Android App

This project is an Android application that connects to an OBD2 Bluetooth dongle, reads vehicle telemetry data, stores it locally, and synchronizes it to Firebase Cloud Firestore using a robust, batched background sync pipeline.

For detailed architecture and module documentation, see `docs/`.

